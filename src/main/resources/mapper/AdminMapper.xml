<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flyingideal.dao.AdminMapper">
    <resultMap id="User" type="com.flyingideal.model.User">
        <result property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="salt" column="salt"/>
        <result property="locked" column="locked"/>
        <result property="source" column="source"/>
        <result property="sourceUserName" column="source_username"/>
        <result property="organizationId" column="organization_id"/>
        <result property="email" column="email"/>
        <result property="telephone" column="telephone"/>
        <result property="birthday" column="birthday"/>
        <result property="gmtCreate" column="gmt_create"/>
        <result property="gmtModified" column="gmt_modified"/>
    </resultMap>

    <select id="getUsers" resultMap="User">
        SELECT id,username,locked,source,source_username,organization_id,email,telephone,birthday,gmt_create,gmt_modified FROM t_user
    </select>

    <insert id="addRole">
        INSERT INTO t_user_role (user_id, role_id, username) VALUE (#{userId}, #{roleId}, #{username})
    </insert>

    <delete id="deleteRole">
        DELETE FROM t_user_role WHERE username = #{username} AND role_id = #{roleId}
    </delete>

</mapper>